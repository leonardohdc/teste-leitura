# Caminho da base SQLite (relativo ao directório de trabalho ao arrancar o Uvicorn, p.ex. backend/)
SQLITE_PATH=./data/app.db

# Tamanho máximo de upload em MB (opcional)
# MAX_UPLOAD_MB=10

# OpenAI (opcional — sem chave, o fluxo continua com regras + SQLite + revisão manual)
OPENAI_API_KEY=
OPENAI_MODEL=gpt-4o-mini
# Confiança mínima para aceitar categoria LLM sem marcar revisão
# LLM_CONFIDENCE_MIN=0.7
